{
  "name": "@helden-inc/tg-schema",
  "version": "2.0.0",
  "description": "Training Game data contract: TypeScript types + Zod validators + SCHEMA_VERSION. Consumed by tg-runtime and tg-cms.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "prepare": "tsc",
    "test": "tsc -p tsconfig.test.json && node dist-test/__examples__/phases.example.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^24.13.2"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
